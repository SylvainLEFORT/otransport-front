import NavBar from '../NavBar';
import './createdriver.scss';

const CreateDriver = () => (
  <div>
    <NavBar />
    <div>
      ici bientot la création d'un chauffeur
    </div>
  </div>
);

export default CreateDriver;
